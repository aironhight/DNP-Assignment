@page
@model VIACinema.Pages.Tickets.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Ticket</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Ticket.Seat" class="control-label"></label>
                <input asp-for="Ticket.Seat" class="form-control" />
                <span asp-validation-for="Ticket.Seat" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Ticket.ScreeningId" class="control-label"></label>
                <select asp-for="Ticket.ScreeningId" class ="form-control" asp-items="ViewBag.ScreeningId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
